<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Public mode (checked) - Brand Secondary Blue -->
    <item android:state_checked="true">
        <shape android:shape="rectangle">
            <solid android:color="@color/brand_secondary"/>
            <corners android:radius="12dp"/>
            <stroke android:width="2dp" android:color="@color/brand_secondary"/>
        </shape>
    </item>
    <!-- Private mode (unchecked) - Elevated surface -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/bg_elevated"/>
            <corners android:radius="12dp"/>
            <stroke android:width="1dp" android:color="@color/card_border"/>
        </shape>
    </item>
</selector>
