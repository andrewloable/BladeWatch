name: bladewatch_ui
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.3.0+13000

environment:
  sdk: ^3.12.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8

  # Runtime for lib/gen/ (generated by protoc-gen-dart via proto/buf.gen.yaml,
  # pinned to v25.1.0 there — keep these in step; see docs/build-and-operations.md).
  protobuf: ^6.1.0
  fixnum: ^1.1.1
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2
  qr_flutter: ^4.1.0
  package_info_plus: ^10.2.1
  flutter_map: ^8.3.2
  latlong2: ^0.10.1
  video_player: ^2.14.0
  webview_flutter: ^4.13.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  video_player_platform_interface: ^6.9.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # PLATFORM: android only.
  #
  # This app targets exactly ONE thing: an arm64 Android head unit (BYD DiLink
  # v3, Android 10 / API 29). There is no BladeWatch on a desktop, phone or
  # browser.
  #
  # This is a COMMENT and not a `platforms:` key on purpose. `platforms:` under
  # `flutter:` is valid for PLUGINS only; on an application Flutter rejects it
  # outright -- `flutter pub get` fails with
  #   Unexpected child "platforms" found under "flutter".
  # (verified, not assumed). So there is no machine-readable way for an app to
  # declare its platform set.
  #
  # The ENFORCEMENT is therefore the `validateFlutterAndroidOnly` Gradle task in
  # app/build.gradle.kts, which fails the build if
  # flutter_ui/{ios,macos,windows,linux,web} reappears, backed by .gitignore
  # entries so a regenerated tree cannot be committed.

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # Runs `flutter gen-l10n` (config in l10n.yaml) automatically before build/
  # analyze, from the ARB files ported from res/values*/strings.xml in
  # BladeWatch-ncbb.3 — see tools/i18n/xml_to_arb.py.
  generate: true

  # Settings → About's license dialog — same file as the native app's
  # app/src/main/assets/LICENSE.txt (BladeWatch-yz1e.3).
  #
  # Vehicle screen's 3D hero (BladeWatch-yz1e.9): a byte-identical copy of
  # the native app's app/src/main/assets/web/hero/hero.html (three.js in a
  # WebView — see docs/build-and-operations.md for why this must stay a
  # WebView, not native 3D) plus its vendor bundle and bundled GLB models,
  # at the same relative paths so hero.html's own `../shared/...` references
  # resolve unmodified under webview_flutter's loadFlutterAsset().
  assets:
    - assets/LICENSE.txt
    # BladeWatch-ez0z: the app icon, for the in-app brand lockup shown while
    # the app starts. Android mipmaps are not reachable from Dart, so the
    # xxxhdpi foreground is shipped as a Flutter asset and scaled down.
    - assets/brand/app_icon.png
    - assets/web/hero/hero.html
    - assets/web/shared/vendor/
    - assets/web/shared/vendor/draco/
    - assets/web/shared/models/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
