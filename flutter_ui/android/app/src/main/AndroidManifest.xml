<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    android:sharedUserId="net.bladewatch.app">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- BladeWatch-yz1e.4: Diagnostics' Network health tile reads the
         connected Wi-Fi SSID (NetworkInfoChannel.kt), same as
         DiagnosticsFragment.kt's computeNetworkTopLine(). -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <application
        android:label="@string/app_name"
        android:name="${applicationName}"
        android:networkSecurityConfig="@xml/network_security_config"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- BladeWatch-x3vw: pin the rendering backend so a Flutter upgrade cannot
             silently change it and invalidate every render-soak result recorded for
             this app.

             Measured on the head unit (BYD AUTO, Adreno 610, Android 10 / API 29):
             the engine probes Vulkan, fails, and falls back —
               android_context_vk_impeller.cc(62)  Using the Impeller rendering backend (Vulkan).
               android_context_gl_impeller.cc(104) Using the Impeller rendering backend (OpenGLES).
             so OpenGLES is what actually renders. We pin what is really running, not
             what Phase 0 hoped for; pinning "vulkan" here would either be ignored on
             this driver or change the measured configuration, and every soak figure
             would have to be re-taken against it.

             The engine turns these two keys into its own enable-impeller and
             impeller-backend command-line switches
             (io.flutter.embedding.engine.FlutterEngineFlags, verified against the
             flutter_embedding 3.44.4 / engine a10d8ac38d jar, not assumed).

             NOTE: OpenGLES on this exact GPU is the driver path the reverted Filament
             port crashed on, so this pin makes the risk explicit and reproducible
             rather than removing it. See docs/build-and-operations.md:58. -->
        <meta-data
            android:name="io.flutter.embedding.android.EnableImpeller"
            android:value="true" />
        <meta-data
            android:name="io.flutter.embedding.android.ImpellerBackend"
            android:value="opengles" />

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
