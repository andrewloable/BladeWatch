<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Theme applied to the Android Window while the process is starting when the OS's Dark Mode setting is off -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <!-- Show a splash screen on the activity. Automatically removed when
             the Flutter engine draws its first frame -->
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>
    <!-- Theme applied to the Android Window as soon as the process has started.
         This theme determines the color of the Android Window while your
         Flutter UI initializes, as well as behind your Flutter UI while its
         running.

         This Theme is only used starting with V2 of Flutter's Android embedding. -->
    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">@drawable/launch_background</item>
        <!-- BladeWatch-ez0z: the SAME drawable as LaunchTheme, not a plain
             colour. LaunchTheme only paints the brief "starting window";
             NormalTheme replaces it as soon as the process starts and stays
             until Flutter paints its first frame. With a plain colour there,
             the icon and wordmark vanished almost immediately and the user saw
             an empty screen for the whole engine init. Flutter draws opaque
             content over this once it is ready. -->
        <!-- BladeWatch-ez0z: explicit, not ?android:colorBackground. This is the
             window behind Flutter while the engine initialises; leaving it on the
             theme attribute is what let a white frame through between the launch
             drawable and Flutter's first paint. -->
    </style>
</resources>
